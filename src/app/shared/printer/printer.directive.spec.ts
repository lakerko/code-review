import { PrinterDirective } from './printer.directive';

describe('PrinterDirective', () => {
  it('should create an instance', () => {
    const directive = new PrinterDirective();
    expect(directive).toBeTruthy();
  });
});
