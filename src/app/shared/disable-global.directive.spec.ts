import { DisableGlobalDirective } from './disable-global.directive';

describe('DisableGlobalDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableGlobalDirective();
    expect(directive).toBeTruthy();
  });
});
